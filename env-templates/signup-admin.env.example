# Signup Admin Dashboard Environment Variables
# Copy this to .env in the signup-admin repository

# Supabase Configuration (must match main webapp)
SUPABASE_URL=https://your-project.supabase.co          # Your Supabase project URL
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key        # Service role key (has full DB access)

# VIP Code Security (must match main webapp)
VIP_PEPPER=your-long-random-string-for-hashing         # Must be same as main webapp VIP_PEPPER

# Admin Authentication
ADMIN_USER=admin                                       # Username for admin dashboard
ADMIN_PASS=your-strong-admin-password-min-16-chars     # Strong password (min 16 chars recommended)

# Security Notes:
# - SUPABASE_SERVICE_ROLE_KEY has full database access, never expose to client
# - VIP_PEPPER must be the same across all systems for code verification
# - ADMIN_PASS should be strong (16+ chars, mixed case, numbers, symbols)
# - Use HTTPS in production for admin dashboard
# - Consider IP whitelisting for admin access