# Account Manager (Hive Signer Microservice) Environment Variables
# Copy this to .env in the account-manager repository

# Hive Blockchain Configuration
HIVE_NODE_URL=https://api.hive.blog                    # Hive RPC node URL
HIVE_CREATOR=your-hive-account-name                    # Account that will create new accounts
HIVE_CREATOR_ACTIVE_WIF=5JYourPrivateKeyHere           # Active private key (51 chars, starts with 5)

# Service Security  
SIGNER_TOKEN=your-secure-token-min-32-characters       # Must match SIGNER_TOKEN in main webapp

# Server Configuration
PORT=3000                                              # Server port
NODE_ENV=production                                    # Environment: development/production/test

# Security Notes:
# - HIVE_CREATOR must have sufficient Resource Credits (RC) to create accounts
# - SIGNER_TOKEN should be a random 32+ character string (use: openssl rand -hex 32)
# - HIVE_CREATOR_ACTIVE_WIF must be exactly 51 characters (Hive WIF format)
# - Never commit these values to version control
# - Use different tokens for different environments